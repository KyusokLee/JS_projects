const userData = document.getElementById('userdata');
const li = document.createElement("li");
const userName = "kevin kim";
const text = document.createTextNode(userName);
li.appendChild(text);
userData.appendChild(li);
